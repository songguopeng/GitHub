#include<iostream>
#include<cmath>
//(1/¡Ì5)*{[(1+¡Ì5)/2]^n - [(1-¡Ì5)/2]^n
using namespace std;
double t = sqrt(5);
int main()
{
	int n;
	while (cin >> n)
		cout << (1 / t)*(pow((1 + t) / 2, n) - pow((1 - t) / 2, n)) << endl<<endl;
	system("pause");
	return 0;
}