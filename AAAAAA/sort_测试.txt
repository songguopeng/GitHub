#include<iostream>
#include<algorithm>
#include<vector>
#include<iterator>
#include<functional>
using namespace std;

template<class T>
bool cmp(T a, T b)
{
	return a > b ? true : false;
}
int main()
{
	vector<int> vt;
	int a;
	A:			//测试使用，提交时勿使用
	vt.clear();
	while (cin >> a&&a!=0)
	{
		vt.push_back(a);
	}
	//sort(vt.begin(), vt.end(),cmp<int>);
	sort(vt.begin(), vt.end(), greater<int>());
	sort(vt.begin(), vt.end(), less<int>());

	copy(vt.begin(), vt.end(), ostream_iterator<int>(cout, "\t"));
	cout << endl;
	
	goto A;
	system("pause");
	return 0;
}