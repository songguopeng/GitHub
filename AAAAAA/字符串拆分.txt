#include<iostream>
#include<string>
#include<sstream>
using namespace std;

//int main()
//{
//	string strText;
//	string strSeparator = " ";
//	string ResultStr;
//	int size_pos = 0;
//	int size_pre_pos = 0;
//	while (cin >> strText)
//	{
//		while ((size_pos = strText.find_first_of(strSeparator, size_pos)) != string::npos)
//		{
//			ResultStr = strText.substr(size_pre_pos, size_pos - size_pre_pos);
//			cout << ResultStr << endl;
//			size_pre_pos = ++size_pos;
//		}
//		if (size_pre_pos != strText.size())
//		{
//			ResultStr = strText.substr(size_pre_pos, size_pos - size_pre_pos);
//			cout << ResultStr << endl;
//		}
//	}
//	system("pause");
//	return 0;
//}

int main()
{
	string strResult = "",str;
	stringstream os;
	while (getline(cin,str,'\n'))
	{
		os.clear();
		os.str("");
		os << str;
		while (!os.eof())
		{
			//os >> strResult;//¿Õ¸ñ
			getline(os, strResult, ',');
			cout << strResult << endl;
		}
	}
	return 0;
}