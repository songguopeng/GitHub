#include<iostream>
#include<algorithm>
#include<vector>
#include<iterator>
#include<functional>

using namespace std;

int main()
{
	vector<int> vt;
	int a;
A:
	vt.clear();
	while (cin >> a&&a != 0)
	{
		vt.push_back(a);
	}
	cout << "ÅÅÐòÇ°£º" << endl;
	copy(vt.begin(), vt.end(), ostream_iterator<int>(cout, "\t"));
	cout << endl;
	cout << "ÅÅÐòºó:" << endl;
	partial_sort(vt.begin(), vt.begin() + 4, vt.end());
	copy(vt.begin(), vt.end(), ostream_iterator<int>(cout, "\t"));
	cout << endl;
	goto A;
	system("pause");
	return 0;

}